{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: file dialogs, reading/writing the Block files the user picks in them, and opening links (About) in the system browser.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "opener:default",
    "fs:allow-exists",
    "fs:allow-read-text-file",
    "fs:allow-write-text-file"
  ]
}
