# Locale files and language menu

Status: open
Blocked by: 05
Spec: ../spec.md

All UI strings come from `locales/en.json`; the language menu lists every `locales/*.json` (built-in and user folder). Missing keys fall back to English. Short "Translating BlockCreator" doc.

**Done when**
- A test `de.json` with a few keys switches those strings; the rest stays English.

**Notes from 05 (editor tracer)**
- Hard-coded English UI strings to move into locale files: `ui/App.tsx` (Slot labels, "Mario", property labels, dot aria-labels), `ui/blockly/categories.ts` (category names), `ui/blockly/toolbox.ts` ("Logic").
